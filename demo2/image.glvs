#version 330 core
layout (location = 0) in vec4 pos;
layout (location = 1) in vec4 tex;

out vec2 texuv;
uniform mat4 mvp;

void main()
{
	gl_Position = mvp * vec4(pos.xyz, 1.0);
	texuv = vec2(tex.x, tex.y);
}

